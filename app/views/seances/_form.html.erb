<%= form_for(seance) do |f| %>
  <% if seance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seance.errors.count, "error") %> prohibited this seance from being saved:</h2>

      <ul>
      <% seance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :projection %>
    <%= f.text_field :projection %>
  </div>

  <div class="field">
    <%= f.label :caisse %>
    <%= f.text_field :caisse %>
  </div>

  <div class="field">
    <%= f.label :horaire %>
    <%= f.datetime_select :horaire %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
