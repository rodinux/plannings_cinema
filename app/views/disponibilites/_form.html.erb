<%= form_for(disponibilite) do |f| %>
  <% if disponibilite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(disponibilite.errors.count, "error") %> prohibited this disponibilite from being saved:</h2>

      <ul>
      <% disponibilite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="container">
  <% users = User.all.users_alphabet.map{|user| user.prenom } %>
  <div class="field">
    <%= f.label :nom, :value => 'Choisissez votre Prénom' %><br>
    <%= f.select :nom, users %>
  </div>
  <h3>En cliquant sur les dates, vous aurez un calendrier pour le choix des dates</h3>
  <h3> surtout remplissez bien ces champs avnat de valider !!</h3>
<h2>Je serai absent</h2>
  <div class="field">
    <%= f.label :start_time, :value => 'Du:' %>
    <%= f.text_field :start_time, :class => "datepicker" %>
  </div>

  <div class="field">
    <%= f.label :end_time, :value => 'Au:' %>
    <%= f.text_field :end_time, :class => "datepicker" %>
  </div>

  <div class="actions">
    <input type="submit" class="btn btn-lg btn-warning" value="Valider" />
  </div>
<% end %>
</div>
<script type="text/javascript">
  $(document).ready(function(){
    $('.datepicker').datepicker({
    format: "dd/mm/yyyy",
    todayBtn: true,
    language: "fr",
    todayHighlight: true
    });
  });
</script>